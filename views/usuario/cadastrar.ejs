<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro de usuário</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
</head>
<body>
  
        <div class="container">
          <div>
            <% if (status == 1) { %>
              <div style="background-color: lightcoral;">E-mail já utilizado</div>
            <% } %>
          <br>
          <h4>Cadastar Usuarios</h4>
     <form action="/usuarios" method="post">
      <input type= "hidden" name="_id" value="<%= usuario._id%>">
        <div class="mb-3">
          <label class="form-label">Nome:</label>
          <input value= "<%= usuario.nome %>" type= "text" class="form-control" name= "nome">
        </div>
        <div class="mb-3">
          <label class="form-label">E-mail:</label>
          <input value= "<%= usuario.email %>" type= "email" class="form-control" name= "email">
        </div>
        <div class="mb-3">
          <label class="form-label">Senha:</label>
          <input <% if(usuario.senha != undefined){ %> disabled <% } %> value= "<%= usuario.senha %>" type= "password" class="form-control" name= "senha">
        </div>
          <input type= "submit" class="btn btn-primary" value="Salvar"></input>
        <a href="/">
          <input type="button" class="bnt bnt-secondary" value="<<Voltar"></a>
          <div>
            <a href="/usuarios/login"> Possui conta?</a>
          </div>
    </form>
        </div>
               <%- include("../partials/rodape.ejs") %><br>
</body>
</html>